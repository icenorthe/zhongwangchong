# PythonAnywhere 上传包说明

这个目录是旧版“手动上传”快照。  
如果你现在使用的是整理后的项目结构，优先参考：

- `docs/PythonAnywhere部署说明.md`

如果只是临时手动上传，可以把这里的文件先传到 PythonAnywhere，再按正式部署说明补完虚拟环境和 ASGI 配置。

提醒：

- 这里的内容是上传用副本，不一定和当前项目结构完全一致
- 更推荐使用 `一键同步云端.bat`，它会按照当前目录结构自动上传所需文件

